 
# Task Manager - نظام إدارة المهام

نظام ذكي لإدارة المهام مبني بـ PHP باستخدام OOP + MVC + Database Relations

## 🎯 المميزات

### الوظائف الأساسية
- ✅ نظام تسجيل دخول وإنشاء حساب آمن
- ✅ إدارة الملف الشخصي مع رفع صورة
- ✅ تنظيم المهام في 3 مستويات:
  - **التصنيفات** (Categories): مثل دراسة، عمل، حياة شخصية
  - **المهام الرئيسية** (Main Tasks): داخل كل تصنيف
  - **المهام الفرعية** (Subtasks): تحت كل مهمة رئيسية
- ✅ تتبع حالة المهام (قيد التنفيذ / مكتملة)
- ✅ واجهة مستخدم عصرية وريسبونسيف (Mobile-First)

### التقنيات المستخدمة
- **Backend**: PHP 7.4+ (OOP + MVC)
- **Database**: MySQL with PDO
- **Frontend**: Bootstrap 5 RTL + Custom CSS
- **Architecture**: 
  - PSR-4 Autoloading
  - Namespaces
  - MVC Pattern
  - Database Relations with JOIN queries

## 📋 المتطلبات

- PHP 7.4 أو أحدث
- MySQL 5.7 أو أحدث
- Apache Web Server مع mod_rewrite
- مكتبة PDO مفعلة

## 🚀 التثبيت

### 1. استنساخ المشروع
```bash
git clone <repository-url>
cd TaskManager
```

### 2. إعداد قاعدة البيانات
1. افتح `config/database.php`
2. عدّل إعدادات الاتصال:
```php
define('DB_HOST', 'localhost');
define('DB_NAME', 'task_manager');
define('DB_USER', 'root');
define('DB_PASS', '');
```

### 3. تثبيت قاعدة البيانات
افتح المتصفح وانتقل إلى:
```
http://localhost/TaskManager/install.php
```

### 4. الوصول للتطبيق
```
http://localhost/TaskManager/public/
```

## 📁 هيكل المشروع

```
TaskManager/
├── app/
│   ├── Controllers/      # الكونترولرات
│   │   ├── AuthController.php
│   │   ├── CategoryController.php
│   │   ├── ProfileController.php
│   │   └── TaskController.php
│   ├── Models/          # الموديلات
│   │   ├── User.php
│   │   ├── Category.php
│   │   ├── MainTask.php
│   │   └── Subtask.php
│   ├── Views/           # الواجهات
│   │   ├── layouts/
│   │   ├── auth/
│   │   ├── profile/
│   │   ├── categories/
│   │   ├── tasks/
│   │   └── errors/
│   ├── Core/            # الكلاسات الأساسية
│   │   ├── Database.php
│   │   ├── Router.php
│   │   ├── Controller.php
│   │   └── Model.php
│   └── Helpers/         # الكلاسات المساعدة
│       ├── Session.php
│       ├── Validator.php
│       └── FileUpload.php
├── config/              # ملفات التكوين
│   └── database.php
├── public/              # الملفات العامة
│   ├── css/
│   ├── js/
│   ├── uploads/
│   └── index.php
├── autoload.php         # PSR-4 Autoloader
├── install.php          # سكريبت التثبيت
└── .htaccess           # URL Rewriting
```

## 🗄️ قاعدة البيانات

### الجداول والعلاقات

#### 1. users
```sql
- id (PK)
- name
- email (UNIQUE)
- password (hashed)
- profile_image
- face_data (للبصمة الوجه - مستقبلي)
- fingerprint_data (لبصمة الإصبع - مستقبلي)
- created_at
- updated_at
```

#### 2. categories
```sql
- id (PK)
- user_id (FK → users.id)
- name
- created_at
- updated_at
```

#### 3. main_tasks
```sql
- id (PK)
- category_id (FK → categories.id)
- title
- description
- status (pending/completed)
- created_at
- updated_at
```

#### 4. subtasks
```sql
- id (PK)
- main_task_id (FK → main_tasks.id)
- title
- status (pending/completed)
- created_at
- updated_at
```

### أمثلة على استعلامات JOIN

**الحصول على التصنيفات مع عدد المهام:**
```sql
SELECT c.*, COUNT(mt.id) as task_count
FROM categories c
LEFT JOIN main_tasks mt ON c.id = mt.category_id
WHERE c.user_id = ?
GROUP BY c.id
```

**الحصول على المهام مع عدد المهام الفرعية:**
```sql
SELECT mt.*, c.name as category_name,
       COUNT(st.id) as subtask_count,
       SUM(CASE WHEN st.status = 'completed' THEN 1 ELSE 0 END) as completed_subtasks
FROM main_tasks mt
INNER JOIN categories c ON mt.category_id = c.id
LEFT JOIN subtasks st ON mt.id = st.main_task_id
WHERE mt.category_id = ?
GROUP BY mt.id
```

## 🔒 الأمان

- ✅ تشفير كلمات المرور باستخدام `password_hash()` (bcrypt)
- ✅ استخدام Prepared Statements لمنع SQL Injection
- ✅ تنظيف المدخلات باستخدام `htmlspecialchars()`
- ✅ التحقق من صحة الملفات المرفوعة
- ✅ التحقق من ملكية البيانات قبل التعديل/الحذف
- ✅ إدارة الجلسات بشكل آمن

## 🎨 التصميم

- **Mobile-First**: مُحسّن للهواتف المحمولة أولاً
- **Responsive**: يعمل على جميع الأجهزة
- **Modern UI**: تصميم عصري مع:
  - ألوان متدرجة (Gradients)
  - ظلال ناعمة (Soft Shadows)
  - بطاقات مستديرة (Rounded Cards)
  - أيقونات Bootstrap Icons
  - خطوط Google Fonts (Inter)

## 🔮 المميزات المستقبلية

- [ ] تسجيل الدخول ببصمة الوجه (Face Recognition)
- [ ] تسجيل الدخول ببصمة الإصبع (Fingerprint)
- [ ] إشعارات المهام
- [ ] تصدير المهام (PDF/Excel)
- [ ] مشاركة المهام مع مستخدمين آخرين
- [ ] تطبيق موبايل (React Native)

## 📝 الاستخدام

### 1. إنشاء حساب
- انتقل إلى صفحة التسجيل
- أدخل الاسم والبريد الإلكتروني وكلمة المرور
- (اختياري) ارفع صورة شخصية

### 2. إنشاء تصنيف
- بعد تسجيل الدخول، اضغط على زر "+"
- أدخل اسم التصنيف (مثل: دراسة)

### 3. إضافة مهمة رئيسية
- افتح التصنيف
- اضغط على زر "+"
- أدخل عنوان المهمة ووصفها

### 4. إضافة مهام فرعية
- افتح المهمة الرئيسية
- أضف المهام الفرعية واحدة تلو الأخرى
- ضع علامة ✓ عند إكمال المهمة

## 👨‍💻 المطور

تم تطويره كمشروع نهائي لمادة تطوير الويب - PHP

## 📄 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام التعليمي.

---
  
